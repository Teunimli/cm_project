const request = require('request');

module.exports = {

    sendVoice(){
        return new Promise((resolve) => {
            let headers = {
                "Content-Type": "application/json",
            };
        });
}